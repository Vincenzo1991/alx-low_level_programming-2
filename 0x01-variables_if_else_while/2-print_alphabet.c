#include <stdio.h>
/**
 * main - prints letters of the alphabet in lower case
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char lower_case = 'a';

	while
		(lower_case <= 'z');

	{
		putchar(lower_case);
		lower_case++;
	}

	putchar('\n');
	return (0);
}
